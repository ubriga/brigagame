{
  "name": "brigagame-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Brigagame 2.0 - Cloudflare Workers port (parallel copy, PA version untouched)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node test/run.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0",
    "@cloudflare/workers-types": "^4.20240620.0"
  }
}